# Generated from an actual import scan of the codebase (grep for every
# top-level `import X` / `from X import ...` across backend/**/*.py,
# filtered down to third-party packages — internal modules like
# `decision`, `oi`, `brokers`, etc. are this project's own packages, not
# dependencies). Versions are minimums known to work with the syntax used
# (e.g. pandas >=2.0 for the DataFrame APIs this project relies on) rather
# than exact pins — tighten these to your actually-tested versions before
# deploying, this is a starting point, not a lockfile.

numpy>=1.26
pandas>=2.0
pyarrow>=14.0
scipy>=1.11
scikit-learn>=1.3
joblib>=1.3

requests>=2.31
urllib3>=2.0
aiohttp>=3.14.3
websocket-client>=1.7
websockets>=12.0
python-dotenv>=1.0
pyotp>=2.9
pytz>=2023.3
orjson>=3.9
logzero>=1.7
xlwings>=0.30

# AngelOne SmartAPI Python SDK — imported as `from SmartApi import
# SmartConnect` in brokers/smartapi_client.py.
smartapi-python>=1.4

# Shoonya execution is optional. Its official SDK is installed separately
# from https://github.com/Shoonya-Dev/ShoonyaApi-py because it is not
# published as a conventional versioned PyPI package.
